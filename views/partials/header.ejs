<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container">
    <a class="navbar-brand fw-bold d-flex align-items-center gap-2" href="/">
  <img src="/img/ourbank-icon.svg" alt="Our Bank" width="32" height="32" />
  <span>Our<span style="color:#10b981;">Bank</span></span>
</a>
    <div class="ms-auto d-flex gap-2">
      <% if (currentUser) { %>
        <% if (currentUser.is_admin) { %>
          <a class="btn btn-light btn-sm" href="/admin/dashboard">Admin</a>
        <% } else { %>
          <a class="btn btn-light btn-sm" href="/dashboard">Mon espace</a>
          <a class="btn btn-light btn-sm" href="/profil">Profil</a>
          <a class="btn btn-light btn-sm" href="/historique">Historique</a>
        <% } %>
        <form action="/logout" method="POST" class="d-inline" id="form-logout">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <button class="btn btn-outline-light btn-sm" type="submit">Déconnexion</button>
        </form>
      <% } else { %>
        <a class="btn btn-light btn-sm" href="/login">Connexion</a>
        <a class="btn btn-outline-light btn-sm" href="/register">Inscription</a>
      <% } %>
    </div>
  </div>
</nav>